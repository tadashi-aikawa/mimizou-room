# [jq] faq


ハイフンを含むkeyを指定できない
-------------------------------

`'.["..."]'`でアクセスする。

```
jq '.["hoge-hoge"]' xxx.json
```

変数の中にハイフンが含まれる場合は、内側をエスケープする。

```
hoge="hoge-hoge"
jq ".[\"${hoge}\"]" xxx.json
```
